zuul:
  routes:
    #  /myProduct/product/list -> /product/product/list
    product:
      path: /myProduct/**
      serviceId: product
      # 获取cookie
      securityHeaders:

      # 简洁写法
#    product: /myProduct/**
  # 排除路径
  ignored-patterns:
    - /**/test
  #
  sensitive-headers:
